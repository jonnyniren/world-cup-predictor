rules_version = '2';
// Open rules for a friends game — anyone can read/write.
// If you want to tighten security later, restrict writes to authenticated users.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
